# 77: On a phone, a Screen mock says it can be swiped

**What to build:** On a phone, every Screen mock tells the visitor they can swipe it sideways to see the whole screen. Today, at 700px wide and narrower, each Screen mock is drawn wider than the phone inside a box that pans sideways, and nothing says so: no words, no faded edge, no scrollbar. A visitor sees a third of the screen and thinks that is all there is. This ticket adds a visible hint and a faded edge. It is also the fallback ticket 79 relies on when a Screen mock has no Phone crop.

**Blocked by:** None (can start immediately).

**Status:** ready-for-agent

- [ ] At 700px wide and narrower, every Screen mock on the home and product pages shows a short hint under or over it: «اسحب لرؤية الشاشة كاملة» in Arabic, "Swipe to see the whole screen" in English
- [ ] The edge where more of the screen is hidden fades out, and the fade moves to the other edge as the visitor swipes, and disappears from an edge once that edge is reached
- [ ] The hint fades away once the visitor has swiped that Screen mock
- [ ] The hint's words are in the CMS with the site's other site-wide words, in both languages
- [ ] Wider than 700px nothing changes
- [ ] The hint is not read out twice by a screen reader and does not replace the Screen mock's description (ADR-0002)
- [ ] Tests cover: the hint and fade present at 390px, absent at 768px, and the hint gone after a sideways swipe

## Comments

> *This was generated by AI during a grilling session with the founder, 23 September 2026.*

## Agent Brief

**Category:** enhancement
**Summary:** Make the phone-width pan area of every Screen mock visibly swipeable.

**Current behaviour (confirmed 23 September 2026):**

- Screen mocks are exported images of a fixed 1440×900 stage (ADR-0002). A picture cannot reflow.
- At 700px and narrower, both the Reference site and the build draw each one 1040px wide inside a box that pans sideways without moving the page. Tickets 08 and 12 made this a requirement and it is tested. At 701px to 980px it only shrinks.
- There is no hint, no fade and no styled scrollbar.

**Desired behaviour (decided by the founder, 23 September 2026):**

- A hint and a faded edge on phones, as above. The founder's wider decision is that phones show a **Phone crop** (CONTEXT.md) instead of the swipeable screen (ticket 78). This ticket stays useful after that: it is what a phone shows whenever a Screen mock has no matching Phone crop (ticket 79).
- **Unchanged:** the 1040px pan itself, the tablet range, and every width above 700px.
