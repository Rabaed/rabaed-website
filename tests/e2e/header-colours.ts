/**
 * The header's background over a dark section and over a light one: the
 * Reference site's two treatments, to the value (ticket 04). A spec that only
 * needs to know which of the two the header is showing reads it from here;
 * `page-shell.spec.ts` holds the full treatments, text and border too.
 */
export const HEADER_BACKGROUND_OVER_DARK = 'rgba(20, 22, 28, 0.72)';
export const HEADER_BACKGROUND_OVER_LIGHT = 'rgba(250, 250, 248, 0.8)';
